const sumNumbers = function (num1: number, num2: number) {
    return num1 + num2;
}

const sumNumbers2 = (num1: number, num2: number): number => num1 + num2; 

alert(sumNumbers(1, 2));

alert(sumNumbers2(3, 4));

console.log(sumNumbers(8, 1));